class Statistics {
    constructor (status, statusDescription, correctTotal, round) {
        this.status = status;
        this.statusDescription = statusDescription;
        this.correctTotal = correctTotal;
        this.round = round;
    }
}

module.exports = Statistics;